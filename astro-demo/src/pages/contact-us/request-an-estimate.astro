---
import Layout from "../../layouts/Layout.astro";
import client from "../../graphql/client/ApolloClient";
import { requestEstimate } from "../../graphql/gql/requestEstimate.gql";
import RequestEstimateMain from "../../components/RequestEstimateMain/RequestEstimateMain";
import type { IRequestAnEstimateQueryResponse } from "../../graphql/generatedType/RequestEstimateTypes";

const { data: requestEstimateData, error: faqError } =
  await client.query<IRequestAnEstimateQueryResponse>({
    query: requestEstimate,
  });
console.log(requestEstimateData, faqError, "requestEstimateData");
---

<Layout>
  <RequestEstimateMain client:load data={requestEstimateData} />
</Layout>
