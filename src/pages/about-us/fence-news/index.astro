---
import BlogListing from "../../../components/BlogListing/BlogListing";
import client from "../../../graphql/client/ApolloClient";
import type { IBlogListing } from "../../../graphql/generatedType/BlogListing.Types";
import { IBlogListingQuery } from "../../../graphql/gql/blogListing.gql";
import Layout from "../../../layouts/Layout.astro";

const { data: blogData, error: blogError } = await client.query<IBlogListing>({
  query: IBlogListingQuery,
});
---

<Layout>
  <BlogListing client:load blogData={blogData} />
</Layout>
